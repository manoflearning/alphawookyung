#include <bits/stdc++.h>
using namespace std;

bitset<33554432> bs;

int main() {

	cin.tie(NULL); cout.tie(NULL);
	ios_base::sync_with_stdio(false);

	while (1) {
		int x; cin >> x;
		if (cin.eof()) break;

		if (bs[x]) continue;
		
		cout << x << ' ';
		bs[x] = 1;
	}

	return 0;
}