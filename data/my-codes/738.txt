#include <iostream>
using namespace std;

int main() {
	int a, b, c;
	char pass;

	scanf("%d %c %d %c %d", &a, &pass, &b, &pass, &c);

	if (a + b == c) printf("YES");
	else printf("NO");

	return 0;
}
