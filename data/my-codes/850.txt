#include <iostream>
#include <algorithm>
using namespace std;

int main() {
	long long a, b;
	scanf("%lld %lld", &a, &b);

	if (a > b) swap(a, b);
	
	long long ans = (a + b) * (b - a + 1) / 2;
	printf("%lld", ans);

	return 0;
}